version 2.0.0;
private_input;
@type field 2;
@begin
    <1>;  // Input 1
    <0>;  // Input 2
    <1>;  // Input 3
    
    // Round constants for the first round
    <1>;  // Round constant 0
    <0>;  // Round constant 1
    <1>;  // Round constant 2
    
    // MDS matrix coefficient for the first round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the second round
    <0>;  // Round constant 3
    <1>;  // Round constant 4
    <0>;  // Round constant 5
    
    // MDS matrix coefficient for the second round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the third round
    <1>;  // Round constant 6
    <0>;  // Round constant 7
    <1>;  // Round constant 8
    
    // MDS matrix coefficient for the third round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the first partial round
    <1>;  // Round constant 9
    <1>;  // Round constant 10
    <1>;  // Round constant 11
    
    // MDS matrix coefficient for the first partial round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the second partial round
    <0>;  // Round constant 12
    <1>;  // Round constant 13
    <0>;  // Round constant 14
    
    // MDS matrix coefficient for the second partial round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the third partial round
    <1>;  // Round constant 15
    <0>;  // Round constant 16
    <1>;  // Round constant 17
    
    // MDS matrix coefficient for the third partial round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the fourth partial round
    <0>;  // Round constant 18
    <1>;  // Round constant 19
    <0>;  // Round constant 20
    
    // MDS matrix coefficient for the fourth partial round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the first final round
    <1>;  // Round constant 21
    <0>;  // Round constant 22
    <1>;  // Round constant 23
    
    // MDS matrix coefficient for the first final round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the second final round
    <0>;  // Round constant 24
    <1>;  // Round constant 25
    <0>;  // Round constant 26
    
    // MDS matrix coefficient for the second final round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
    
    // Round constants for the third final round
    <1>;  // Round constant 27
    <0>;  // Round constant 28
    <1>;  // Round constant 29
    
    // MDS matrix coefficient for the third final round
    <0>;   // Coefficient 2 for MDS matrix (will be converted to 2 in F64b)
@end
